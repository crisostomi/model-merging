
export http_proxy='http://login01::3133'
export https_proxy='http://login01:3133'
# Uppercase variants (many tools expect these)
export HTTP_PROXY="$http_proxy"
export HTTPS_PROXY="$https_proxy"

# RHEL/Rocky/CentOS family (likely on Leonardo)
export SSL_CERT_FILE=/etc/pki/tls/certs/ca-bundle.crt

# If the above path does NOT exist, try the Debian/Ubuntu path:
# export SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt

# Requests honors this too; many libs use it
export REQUESTS_CA_BUNDLE="$SSL_CERT_FILE"

# (Optional) git and pip also have their own envs:
export GIT_SSL_CAINFO="$SSL_CERT_FILE"
export PIP_CERT="$SSL_CERT_FILE"


export HF_HOME="/leonardo_work/IscrC_LENS/dcrisost/caches"
export TRANSFORMERS_CACHE="/leonardo_work/IscrC_LENS/dcrisost/caches"
export HF_DATASETS_CACHE="/leonardo_work/IscrC_LENS/dcrisost/caches/datasets"